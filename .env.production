# Loaded by Vite for production builds (Vercel). Public URL, not a secret — this
# is what makes the deployed site fetch content + transcribe from the live
# backend. Local dev uses .env instead.
VITE_API_BASE=https://mandarin-api.ethanadams.dev
